import React, { useState } from 'react'
import { Map, TileLayer, Marker, Popup } from 'react-leaflet'
import { Button } from 'antd'

const Screen = () => {
    return (
        <div>
            <h1>Single Stop screen</h1>
        </div>
    )
}

export default Screen